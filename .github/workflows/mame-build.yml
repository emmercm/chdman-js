# Requires repo secret: PERSONAL_ACCESS_TOKEN with permissions:
#   Contents: read and write
#   Pull Requests: read and write

name: MAME Build

on:
  workflow_dispatch:
    inputs:
      mame_ref:
        description: 'MAME GitHub branch, tag, or SHA (e.g. "mame0281")'
        required: true
        type: string
      auto-merge:
        description: 'Auto-merge pull request?'
        required: true
        type: boolean
        default: true
  push:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

env:
  CHDMANJS_REF: ${{ github.head_ref || github.ref }}

  MAME_REF: ${{ inputs.mame_ref || 'mame0287' }}
  # `make` options
  EMULATOR: 0
  TOOLS: 1
  TESTS: 0
  IGNORE_GIT: 1
  NOWERROR: 1
  DEBUG: 0
  SYMBOLS: 0
  OPTIMIZE: s

jobs:
  build-linux:
    name: build-linux ${{ matrix.nodejs_arch }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            docker_image: i386/alpine:latest
            docker_arch: linux/386
            nodejs_arch: ia32
          - os: ubuntu-latest
            docker_image: alpine:latest
            docker_arch: linux/amd64
            nodejs_arch: x64
          - os: ubuntu-latest
            docker_image: alpine:latest
            docker_arch: linux/arm/v7
            archopts: -mfpu=neon
            nodejs_arch: arm
          - os: ubuntu-latest
            docker_image: alpine:latest
            docker_arch: linux/arm64/v8
            nodejs_arch: arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          repository: mamedev/mame
          ref: ${{ env.MAME_REF }}
      - uses: docker/setup-qemu-action@v3
      - name: Build
        uses: addnab/docker-run-action@v3
        with:
          image: ${{ matrix.docker_image }}
          shell: sh
          options: |
            --platform ${{ matrix.docker_arch }}
            --volume ${{ github.workspace }}:/build
            --workdir /build
            --env EMULATOR
            --env TOOLS
            --env TESTS
            --env IGNORE_GIT
            --env NOWERROR
            --env DEBUG
            --env SYMBOLS
            --env OPTIMIZE
          run: |
            set -euxo pipefail
            apk add --no-cache build-base python3 sdl2-dev sdl2_ttf-dev fontconfig-dev pulseaudio-dev qt5-qtbase-dev
            make VERBOSE=1 -j$(nproc) -r \
              TARGETOS=linux \
              ARCHOPTS='-U_FORTIFY_SOURCE -ffunction-sections -fdata-sections -flto ${{ matrix.archopts }}' \
              LDOPTS='-static -Wl,--gc-sections -flto'

            # Test binary
            apk add --no-cache file binutils
            ./chdman --help || true
            file chdman
            readelf -d chdman || true
            if readelf -d chdman | grep -q NEEDED; then
              echo "ERROR: binary has dynamic dependencies, expected fully static"
              readelf -d chdman | grep NEEDED
              exit 1
            fi
            if ! file chdman | grep -qE 'statically linked|static-pie linked'; then
              echo "ERROR: 'file' does not report the binary as statically linked"
              exit 1
            fi

            # Strip binary
            apk add --no-cache binutils
            ls -al chdman
            strip chdman
            ls -al chdman
      - name: Rename binary
        run: |
          dir="artifacts/packages/chdman-linux-${{ matrix.nodejs_arch }}"
          mkdir -p "${dir}"
          mv chdman "${dir}/"
          ls -al "${dir}/"
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ runner.os }}-${{ matrix.nodejs_arch }}-${{ github.sha }}
          path: artifacts/**/chdman
          if-no-files-found: error

  # https://github.com/mamedev/mame/blob/4505d5459fed086d35f08d7572cb55cd661cd707/.github/workflows/ci-macos.yml
  build-macos:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-15-intel, macos-15]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          repository: mamedev/mame
          ref: ${{ env.MAME_REF }}
      - name: Install dependencies
        run: brew install --overwrite python3 sdl2 sdl3
      - name: Build
        env:
          USE_LIBSDL: 1
          ARCHOPTS: "-ffunction-sections -fdata-sections -flto -Wl,-dead_strip"
        run: make "-j$(sysctl -n hw.physicalcpu)" -r
      - name: Make portable
        run: |
          set -x
          otool -L chdman
          otool -L chdman | awk '{print $1}' | grep -E "^$(brew --prefix)" | while read -r file; do
            cp "${file}" ./
            install_name_tool -change "${file}" "@executable_path/$(basename "${file}")" chdman
          done
          brew uninstall --ignore-dependencies sdl2 sdl3
      - name: Test binary
        run: |
          set -x
          ./chdman --help || true
          file chdman
          otool -L chdman
      - name: Strip binary
        run: |
          ls -al chdman
          strip chdman
          ls -al chdman
      - uses: actions/setup-node@v4
      - name: Rename binary
        run: |
          # shellcheck disable=SC2016
          dir="artifacts/packages/chdman-$(node --eval 'console.log(`${process.platform}-${process.arch}`)')"
          mkdir -p "${dir}"
          mv chdman ./*.dylib "${dir}/"
          ls -al "${dir}/"
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ runner.os }}-${{ matrix.os }}-${{ github.sha }}
          path: |
            artifacts/**/chdman
            artifacts/**/*.dylib
          if-no-files-found: error

  # https://github.com/mamedev/mame/blob/4505d5459fed086d35f08d7572cb55cd661cd707/.github/workflows/ci-windows.yml
  build-windows:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            msystem: mingw64
            nodejs_arch: x64
          # Windows ARM64 builds started being published with MAME v0.281 (Sep 2025)
          - os: windows-11-arm
            msystem: clangarm64
            nodejs_arch: arm64
            pacboy: gcc-compat:p
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v4
        with:
          repository: mamedev/mame
          ref: ${{ env.MAME_REF }}
      - uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.msystem }}
          install: curl git make
          pacboy: clang:p gcc:p libc++:p lld:p python:p llvm:p ${{ matrix.pacboy }}
      - run: |
          set -x
          echo $PATH
          python --version
          python3 --version
      - name: Build
        env:
          OVERRIDE_AR: "llvm-ar"
          OVERRIDE_CC: clang
          OVERRIDE_CXX: clang++
          ARCHOPTS: "-fuse-ld=lld -ffunction-sections -fdata-sections -flto -Wl,--gc-sections"
        run: make VERBOSE=1 -j$(nproc) -r
      - name: Test binary
        run: |
          ./chdman.exe --help || true
      - name: Strip binary
        run: |
          ls -al chdman.exe
          strip chdman.exe
          ls -al chdman.exe
      - name: Rename binary
        run: |
          dir="artifacts/packages/chdman-win32-${{ matrix.nodejs_arch }}"
          mkdir -p "${dir}"
          mv chdman.exe "${dir}/"
          ls -al "${dir}/"
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ runner.os }}-${{ matrix.msystem }}-${{ github.sha }}
          path: artifacts/**/chdman.exe
          if-no-files-found: error
